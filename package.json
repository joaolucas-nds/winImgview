{
  "name": "winimgview",
  "version": "0.2.0",
  "description": "Viewer de imagens para Windows — SVG vetorial + WebP",
  "scripts": {
    "dev":   "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
